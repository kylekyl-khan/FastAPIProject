# FastAPI session 用的 secret key，請務必自行更換為隨機字串
SECRET_KEY=some-random-string-for-session

# Azure AD / Microsoft Entra 設定
# --------------------------------------------------
# 請到 Microsoft Entra admin center 建立 App registration，
# 將得到的資訊填入下列欄位。

AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_TENANT_ID=yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy
AZURE_CLIENT_SECRET=your-secret-value

# Redirect URI 必須與 App registration 中設定的一致
# 開發環境（依 README HTTPS 設定）示例：
AZURE_REDIRECT_URI=https://127.0.0.1:8443/auth/callback

# 若不填，程式會根據 AZURE_TENANT_ID 組出預設值：
# https://login.microsoftonline.com/{AZURE_TENANT_ID}
# AZURE_AUTHORITY=https://login.microsoftonline.com/yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy

# SQL Server 連線設定（通訊錄 / Interinfo_Member）
DB_SERVER=your-sql-server
DB_NAME=DB_Mis_Admin
DB_USERNAME=your-db-user
DB_PASSWORD=your-strong-password
